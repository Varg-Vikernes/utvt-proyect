// authUtils.js
export const isUserLoggedIn = (response) => {
    // Verifica si el usuario está autenticado basado en la respuesta del servidor.
  };
  
  export const isUserAdmin = (response) => {
    // Verifica si el usuario es un administrador basado en la respuesta del servidor.
  };
  